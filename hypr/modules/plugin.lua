hl.config({
	-- Your other configurations (input, general, decoration, etc.) go here

	plugin = {
		hyprwinwrap = {
			-- class is an EXACT match and NOT a regex!
			class = "wallpaper-bg",
		},
	},
})
