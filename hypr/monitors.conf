# Managed by monitui — https://github.com/nathaniel-fargo/monitui
# Manual edits will be overwritten on next apply.
# Disabled monitors are not persisted; they are applied at runtime only.

monitor = eDP-2, preferred, 0x1080, 1.500000, transform, 0
monitor = HDMI-A-1, preferred, 0x0, 1, transform, 0
